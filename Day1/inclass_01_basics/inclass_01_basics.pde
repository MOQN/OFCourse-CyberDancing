void setup() {
  // once
  size(400, 500);
  background(100);
  
  ///// COLOR /////
  // color range: 0 - 255
  // (w)
  // (w, a)
  // (r, g, b)
  // (r, g, b, a)
  
  stroke(0,0,255);
  strokeWeight(10);
  fill(255,255,0);
  
  ellipse(width/2, height/2, 250, 250);
}


void draw() {
  // loop
}
