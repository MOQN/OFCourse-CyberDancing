int[] x = {10, 20, 30, 40, 50, 60};

// [] = array access operator
// [i] = i --> index number!
// ***** the first index number is 0 *****
// ***** array.length --> the total number of elements in the array *****
// ***** the last index number is "arr.length - 1" *****

String[] greetings = {"Hi", "Hello", "Nihao", "Ciao", "Hehe" };

//println(greetings[3]);
//println(greetings[greetings.length-1]);

// arrays <3 for-loop
for (int i=0; i<greetings.length; i++) {
  println(greetings[i]);
}
// this is how to iterate the whole elements in the array!
